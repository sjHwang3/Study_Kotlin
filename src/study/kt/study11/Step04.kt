package study.kt.study11

fun main() {

    /**
     *
     * 1) internal은 바이트 코드 상 public이 된다.
     * 때문에 Java 코드에서는 Kotlin 모듈의 internal 코드를 가져올 수 있다.
     *
     * 2) Kotlin proteced와 Java의 protected는 다르다.
     * Java는 같은 패키지의 Kotlin protected 멤버에 접근 할 수 있다.
     *
     **/

}
