package study.kt.study10

fun main() {
    /**
     * 1) final : override를 할 수 없게 한다. default로 보이지 않게 존재한다.
     * 2) open : override를 열어준다.
     * 3) abstract : 반드시 override를 해야한다.
     * 4) override : 상위 타입을 override한다.
     **/
}
